/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.IO;
using System.Net.Http.Headers;
using System.Text.RegularExpressions;

namespace OutSystems.Internal.REST {
    /// <summary>
    /// Internal class used to perform multipart-related methods.
    /// </summary>
    public static class MultipartHelper {
        /// <summary>
        /// Converts a Stream to byte array
        /// </summary>
        /// <param name="stream"></param>
        public static byte[] ToByteArray(this Stream stream) {
            stream.Position = 0;
            byte[] buffer = new byte[stream.Length];
            for (int totalBytesCopied = 0; totalBytesCopied < stream.Length;) {
                totalBytesCopied += stream.Read(buffer, totalBytesCopied, Convert.ToInt32(stream.Length) - totalBytesCopied);
            }
            return buffer;
        }

        /// <summary>
        /// Compares two multipart payloads excluding boundaries
        /// </summary>
        /// <param name="oldMessageText"></param>
        /// <param name="newMessageText"></param>
        /// <param name="newBoundary"></param>
        public static bool EqualsIgnoreBoundaries(string oldMessageText, string newMessageText, out string newBoundary) {
            Regex rx = new Regex("(?<=--)(.*)(?=--)");
            newBoundary = null;
            Match oldMatch = rx.Match(oldMessageText);
            Match newMatch = rx.Match(newMessageText);
            if (oldMatch.Success && newMatch.Success) {
                var oldBoundary = oldMatch.Value;
                newBoundary = newMatch.Value;
                oldMessageText = Regex.Replace(oldMessageText, oldBoundary, newBoundary);
                return oldMessageText.Equals(newMessageText);
            }
            return false;
        }

        /// <summary>
        /// Get media type header value from the content-type property
        /// </summary>
        /// <param name="contentType"></param>
        public static MediaTypeHeaderValue GetMediaTypeHeaderInStringContent(string contentType) {
            try {
                return new MediaTypeHeaderValue(contentType);
            } catch {
                return new MediaTypeHeaderValue("text/plain");
            }
        }

        /// <summary>
        /// Get media type header value from the content-type property
        /// </summary>
        /// <param name="contentType"></param>
        public static MediaTypeHeaderValue GetMediaTypeHeaderInStreamContent(string contentType) {
            try {
                return new MediaTypeHeaderValue(contentType);
            } catch {
                return new MediaTypeHeaderValue("application/octet-stream");
            }
        }
    }
}
