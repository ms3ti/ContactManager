/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.Collections.Generic;
using OutSystems.HubEdition.DatabaseAbstractionLayer.ConfigurationService.DynamicTracing.NotificationPlugin;

namespace OutSystems.HubEdition.DatabaseAbstractionLayer.ConfigurationService.DynamicTracing {
    public abstract class BaseNotifier<TNotifier> : INotifier where TNotifier : class, new() {
        protected event Action<IDictionary<string, string>> OnChangeSubscribers;
        protected event Action<IDictionary<string, string>> OnDeleteSubscribers;

        private static TNotifier notifierInstance;
        public static TNotifier Instance { get {
                if (notifierInstance == null) {
                    notifierInstance = new TNotifier();
                }
                return notifierInstance;
            } 
        }

        // This is here because of new() constraint making the constructor public
        // since currently we only have the FileSystemWatcher, it doesn't make much sense
        // to implement an abstract factory for just one class, if more are created consider the design pattern
        protected BaseNotifier() {
            if (notifierInstance != null) {
                throw new TypeAccessException("Watcher can only be instanced once. Use the Instance");
            }
        }

        public void NotifyChangeSubscribers(IDictionary<string,string> map) {
            OnChangeSubscribers?.Invoke(map);
        }

        public void NotifyDeleteSubscribers(IDictionary<string, string> map) {
            OnDeleteSubscribers?.Invoke(map);
        }

        public void SubscribeToChange(Action<IDictionary<string,string>> action) {
            OnChangeSubscribers -= action;
            OnChangeSubscribers += action;
        }
        public void SubscribeToDelete(Action<IDictionary<string, string>> action) {
            OnDeleteSubscribers -= action;
            OnDeleteSubscribers += action;
        }

        public abstract void StartNotifying(string topic, bool fromError = false);

        public void UnsubscribeToChange(Action<IDictionary<string, string>> action) {
            OnChangeSubscribers -= action;
        }

        public void UnsubscribeToDelete(Action<IDictionary<string, string>> action) {
            OnDeleteSubscribers -= action;
        }
    }
}
