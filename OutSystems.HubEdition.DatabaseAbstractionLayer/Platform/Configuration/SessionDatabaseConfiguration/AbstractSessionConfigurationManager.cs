/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System.Collections.Generic;
using OutSystems.HubEdition.DatabaseAbstractionLayer.Platform.Configuration.ScriptParser;

namespace OutSystems.HubEdition.Extensibility.Data.Platform.Configuration {

    public abstract class AbstractSessionConfigurationManager : BaseConfigurationManager, ISessionConfigurationManager {

        protected ISessionDatabaseConfiguration sessionConfiguration;

        protected const string TAG_ROWCOUNT = "[ROWCOUNT]";
        protected const string TAG_VARSROWCOUNT = "[VARSROWCOUNT]";
        protected const string TAG_CATALOG = "[CATALOG]";
        protected const string TAG_USERNAME = "[USERNAME]";
        protected const string TAG_PASSWORD = "[PASSWORD]";
        protected const string TAG_AUTH = "[AUTH]";
        protected const string TAG_OSADMINUSERNAME = "[OSADMIN_USERNAME]";

        public AbstractSessionConfigurationManager(ISessionDatabaseConfiguration sessionConfiguration) {
            this.sessionConfiguration = sessionConfiguration;
        }

        public abstract IEnumerable<string> SessionStatements { get; }

        public virtual bool RequiresElevatedPrivileges() {
            return sessionConfiguration.ImplementsElevatedPrivilegesOperations;
        }

        public abstract IEnumerable<IDatabaseScriptFile> GenerateCreationScripts();
     

        public abstract void Pre_CreateOrUpgradeSession();

        public virtual bool TestSessionConnection(out string friendlyMessage) {
            bool success = TestConnection(sessionConfiguration.RuntimeDatabaseConfiguration(), out friendlyMessage);
            if (!success) {
                string upperCaseUser = ((AbstractSessionDatabaseConfiguration)sessionConfiguration).SessionUser;
                ((AbstractSessionDatabaseConfiguration)sessionConfiguration).SessionUser = upperCaseUser.ToUpperInvariant();
                success = TestConnection(sessionConfiguration.RuntimeDatabaseConfiguration(), out string newFriendlyMessage);
                if (!success) {
                    ((AbstractSessionDatabaseConfiguration)sessionConfiguration).SessionUser = upperCaseUser;
                } else {
                    friendlyMessage = newFriendlyMessage;
                }
            }
            return success;
        }

        public virtual int QueryTimeout { get; set; }

        public abstract IEnumerable<IDatabaseScriptFile> CreationScriptFiles { get; }

        public abstract IEnumerable<IDatabaseScriptFile> ModelScriptFiles { get; }

        public IEnumerable<IDatabaseScriptFile> AsyncIndexesScriptFiles => throw new System.NotImplementedException();
    }
}