/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.IO;
using System.Text;

namespace OutSystems.HubEdition.DatabaseAbstractionLayer.Platform.Configuration.ScriptParser {
    public class DatabaseScriptFile: IDatabaseScriptFile {
        readonly string FileName;
        readonly string FilePath;
        readonly Encoding EncodingFormat;

        public DatabaseScriptFile(string filepath, string filename, Encoding encoding = null) {
            FileName = filename;
            FilePath = filepath;
            EncodingFormat = encoding ?? Encoding.UTF8;
            
            
        }

        /// <summary>
        /// Returns a FileStream disposable object.
        /// </summary>
        /// <param name="mode">Uses the enum FileMode to specify how the file should be open</param>
        /// <param name="access">Uses the enum FileAccess to specify how the file will be accessed</param>
        /// <returns></returns>
        public FileStream GetFileStream(FileMode mode = FileMode.Open, FileAccess access = FileAccess.Read) {
            try { 
            return new FileStream(Path.Combine(FilePath, FileName), mode, access);
            } catch (Exception e) {
                //Prettify message
                if (e is DirectoryNotFoundException || e is FileNotFoundException) {
                    throw new FileNotFoundException($"Unable to retrieve '{FileName}' from '{FilePath}'"); 
                }
                throw; 
            }
        }

        public string ReadScriptFile() {
            using (var fsSource = GetFileStream()) {
                using (var reader = new StreamReader(fsSource, EncodingFormat)) {
                    return reader.ReadToEnd();
                }
            }
        }

        public string GetFilePath() {
            return FilePath;
        }

        public string GetFileName() {
            return FileName;
        }

        public Encoding GetEncoding() {
            return EncodingFormat;
        }
    }
}
