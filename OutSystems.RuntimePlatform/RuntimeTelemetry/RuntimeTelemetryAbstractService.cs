/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using OutSystems.Internal.Db;
using OutSystems.Logging;
using OutSystems.Logging.LogDefinition;
using Newtonsoft.Json;
using System.Diagnostics.CodeAnalysis;
using OutSystems.RuntimeCommon.Settings;
using OutSystems.RuntimeSettingsConfiguration;
using OutSystems.RuntimeCommon;

namespace OutSystems.HubEdition.RuntimePlatform.RuntimeTelemetry {
    public abstract class RuntimeTelemetryAbstractService<T> where T : RuntimeTelemetryAbstractModel, new() {

        protected T telemetryModel;
        protected readonly ISettingsProvider settingsProvider;

        protected RuntimeTelemetryAbstractService(ISettingsProvider settingsProvider) {
            this.telemetryModel = new T();
            this.settingsProvider = settingsProvider;
        }

        public void Store(T model) {


        }

        protected CustomLogDefinition BuildLog(T model, CustomLogDefinition log, IAppInfo info) {

            // Validations
            if (model == null) {
                throw new ArgumentNullException(nameof(model));
            }

            if (string.IsNullOrEmpty(model.EventType)) {
                throw new InvalidOperationException($"Field {nameof(model.EventType)} cannot be null or empty.");
            }

            if (string.IsNullOrEmpty(model.EventDescription)) {
                throw new InvalidOperationException($"Field {nameof(model.EventDescription)} cannot be null or empty.");
            }

            // Get contextual information
            model.ESpaceKey = info.eSpaceId.ToString();
            model.ESpaceName = info.ApplicationName;
            model.EventTime = DateTime.UtcNow;

            // Add fixed fields
            log.Fields.Add(nameof(model.EventType), new CustomLogDefinition.CustomLogFieldDefinition(model.EventType, false, true));
            log.Fields.Add(nameof(model.EventDescription), new CustomLogDefinition.CustomLogFieldDefinition(model.EventDescription, false, true));
            log.Fields.Add(nameof(model.EventVersion), new CustomLogDefinition.CustomLogFieldDefinition(model.EventVersion, false, true));

            // Serialize telemetry
            var serializedEventInfo = JsonConvert.SerializeObject(model);
            log.Fields.Add(RuntimeTelemetryConstants.EventInfoColumnName, new CustomLogDefinition.CustomLogFieldDefinition(serializedEventInfo, false, false));


            return log;
        }
    }
}
