/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.Linq;
using System.Web.UI;
using OutSystems.HubEdition.RuntimePlatform.Web;
using OutSystems.RuntimeCommon;

namespace OutSystems.HubEdition.WebWidgets {
    public sealed class Body : ViewStateSpecializations.ViewStateLessHtmlGenericControl, IOSControl {

        public Body() : base("body") { }
        public Body(string tagName) : base("body") { }

        private const string CookiePolicyAttributeName = "data-os-cookie-policy";

        protected override void OnDataBinding(EventArgs e) {

            try {
                var options = OutSystems.HubEdition.RuntimePlatform.Internal.HttpHelper.GetDefaultCookieOptions();

                var optionsList = Enumerable.Empty<string>();

                if (options.SameSite != null && options.SameSite != RuntimePlatform.Internal.HttpHelper.SameSiteMode.Unspecified) {
                    optionsList = optionsList.Concat($"SameSite={options.SameSite}");
                }

                if (options.Secure == true) {
                    optionsList = optionsList.Concat("Secure");
                }

                var optionsString = optionsList.StrCat(";");
                if (!string.IsNullOrEmpty(optionsString)) {
                    Attributes[CookiePolicyAttributeName] = optionsString;
                }

            } catch (Exception ex) {
                // Failing getting the cookie options here is not critical enouth to make applications rendering fail. So just log the error to traces.
                OSTrace.Error("Failed calculating default cookie options.", ex);
            }

            base.OnDataBinding(e);
        }

        protected override void Render(HtmlTextWriter writer) {
            RenderBeginTag(writer);
            RenderChildren(writer);
            RenderEndTag(writer);
        }

        // Fix for stackoverflow on exceptions during DataBind
        protected override void DataBindChildren() {
            if (!this.HasControls())
                return;

            int count = this.Controls.Count;
            for (int index = 0; index < count; ++index)
                this.Controls[index].DataBind();
        }

        #region IOSControl members
        string[] IOSControl.CssClass { get { return (this.Attributes["class"] ?? string.Empty).Split(' '); } }
        #endregion
    }
}
