/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.Net;
using System.Web.Http;
using Newtonsoft.Json;
using Newtonsoft.Json.Serialization;
using OutSystems.HubEdition.RuntimePlatform;
using OutSystems.HubEdition.RuntimePlatform.Log;
using OutSystems.RuntimeCommon;

namespace OutSystems.RESTService.Controllers.PWA {
    public class PWAController : RestServiceApiController {

        private readonly JsonSerializerSettings jsonSerializerSettings = new JsonSerializerSettings {
            ContractResolver = new CamelCasePropertyNamesContractResolver(),
            NullValueHandling = NullValueHandling.Ignore,
            Formatting = Formatting.Indented
        };

        protected virtual IAppInfo AppInfo => HubEdition.RuntimePlatform.AppInfo.GetAppInfo();

        protected IHttpActionResult GetManifest() {
            try {
                OSTrace.Info("PWA manifest requested");
                if (AppInfo == null) {
                    throw new ArgumentException("App info can't be empty", nameof(AppInfo));
                }

                var applicationSettings = new ApplicationInfoSettings();
                var manifest = PWAManifest.Create(AppInfo.eSpaceName, applicationSettings);

                return Json(manifest, jsonSerializerSettings);
            } catch (ArgumentException ex) {
                var info = AppInfo as AppInfo;
                ErrorLog.LogApplicationError(ex, info, info?.OsContext, "PWA Manifest");
                return GetErrorResponseResult(ex.Message, HttpStatusCode.BadRequest);
            } catch (Exception ex) {
                var info = AppInfo as AppInfo;
                ErrorLog.LogApplicationError(ex, info, info?.OsContext, "PWA Manifest");
                return GetErrorResponseResult("Internal Server Error", HttpStatusCode.InternalServerError);
            }
        }
    }
}