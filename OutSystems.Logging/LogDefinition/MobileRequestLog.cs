/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using OutSystems.Logging.LogDefinition.Attributes;
using OutSystems.ObjectKeys;
using OutSystems.RuntimeCommon;

namespace OutSystems.Logging.LogDefinition {
    [Serializable]
    [LogType("mobilerequests")]
    public class MobileRequestLogDefinition : AbstractLogDefinition {
        public const int MAX_ID_SIZE = 50;
        public const int MAX_SCREEN_SIZE = 1000;
        public const int MAX_ENDPOINT_SIZE = 1000;
        public const int MAX_SOURCE_SIZE = 1000;
        public const int MAX_ERROR_ID_SIZE = 50;
        public const int MAX_LOGINID_SIZE = 60;


        private string id;
        public string Id {
            get { return id ?? string.Empty; }
            set { id = NormalizeLogField(value, MAX_ID_SIZE); }
        }
        public DateTime Instant { get; set; }
        public int EspaceId { get; set; }
        public int TenantId { get; set; }
        
        private string screen;
        public string Screen {
            get { return screen ?? string.Empty; }
            set { screen = NormalizeLogField(value, MAX_SCREEN_SIZE); }
        }

        private string endpoint;
        public string Endpoint {
            get { return endpoint ?? string.Empty; }
            set { endpoint = NormalizeLogField(value, MAX_ENDPOINT_SIZE); }
        }

        private string source;
        public string Source {
            get { return source ?? string.Empty; }
            set { source = NormalizeLogField(value, MAX_SOURCE_SIZE); }
        }

        public int Duration { get; set; }

        public string ExecutedBy { get; set; }
        
        private string errorId;
        public string ErrorId {
            get { return errorId ?? string.Empty; }
            set { errorId = NormalizeLogField(value, MAX_ERROR_ID_SIZE); }
        }
        
        public string RequestKey { get; set; }
        
        private string loginId;
        public string LoginId {
            get { return loginId ?? string.Empty; }
            set { loginId = NormalizeLogField(value, MAX_LOGINID_SIZE); }
        }
        public int UserId { get; set; }
        public string EspaceName { get; set; }
        public string ApplicationName { get; set; }
        public string ApplicationKey { get; set; }
        public string Username { get; set; }
        public bool HasDetail { get; set; }
        public LogAccess.Type AccessType { get; set; }
        public LogAccess.Visibility AccessVisibility { get; set; }

        public MobileRequestLogDefinition() {
            Id = GenerateLogId();
        }

        public MobileRequestLogDefinition(DateTime instant, int espaceId, int tenantId, string screen, string endpoint,
            string source, int duration, string executedBy, string errorId, string requestKey, string loginId, int userId,
            string eSpaceName, string applicationName, string applicationKey, string username, bool hasDetail = false,
            LogAccess.Type accessType = LogAccess.Type.Runtime, LogAccess.Visibility accessVisibility = LogAccess.Visibility.Internal) :
            this(GenerateLogId(), instant, espaceId, tenantId, screen, endpoint, source, duration, executedBy, errorId, requestKey, loginId, userId,
                eSpaceName, applicationName, applicationKey, username, hasDetail, accessType, accessVisibility) {
        }

        public MobileRequestLogDefinition(MobileRequestLogDefinition obj) :
            this(obj.Instant, obj.EspaceId, obj.TenantId, obj.Screen, obj.Endpoint, obj.Source,
                    obj.Duration, obj.ExecutedBy, obj.ErrorId, obj.RequestKey, obj.LoginId, obj.UserId,
                    obj.EspaceName, obj.ApplicationName, obj.ApplicationKey, obj.Username, obj.HasDetail, obj.AccessType, obj.AccessVisibility) {
        }

        public MobileRequestLogDefinition(string id, DateTime instant, int espaceId, int tenantId, string screen, string endpoint,
            string source, int duration, string executedBy, string errorId, string requestKey, string loginId, int userId,
            string eSpaceName, string applicationName, string applicationKey, string username, bool hasDetail = false,
            LogAccess.Type accessType = LogAccess.Type.Runtime, LogAccess.Visibility accessVisibility = LogAccess.Visibility.Internal) {
            Id = id;
            Instant = instant;
            EspaceId = espaceId;
            TenantId = tenantId;
            Screen = screen;
            Endpoint = endpoint;
            Source = source;
            Duration = duration;
            ExecutedBy = executedBy;
            ErrorId = errorId;
            LoginId = loginId;
            UserId = userId;
            RequestKey = requestKey;
            EspaceName = eSpaceName;
            ApplicationName = applicationName;
            ApplicationKey = applicationKey;
            Username = username;
            HasDetail = hasDetail;
            AccessType = accessType;
            AccessVisibility = accessVisibility;
        }

        public override object Clone() {
            return new MobileRequestLogDefinition(this);
        }

        public void Write(string id, DateTime instant, int espaceId, int tenantId, string screen, string endpoint, string source,
            int duration, string executedBy, string errorId, string loginId, int userId, string requestKey,
            string eSpaceName, string applicationName, ObjectKey applicationKey, string username, bool hasDetail = false,
            LogAccess.Type accessType = LogAccess.Type.Runtime, LogAccess.Visibility accessVisibility = LogAccess.Visibility.Internal) {
            Id = id;
            Instant = instant;
            EspaceId = espaceId;
            TenantId = tenantId;
            Screen = screen;
            Endpoint = endpoint;
            Source = source;
            Duration = duration;
            ExecutedBy = executedBy;
            ErrorId = errorId;
            LoginId = loginId;
            UserId = userId;
            RequestKey = requestKey;
            EspaceName = eSpaceName;
            ApplicationName = applicationName;
            ApplicationKey = ObjectKeyUtils.DatabaseValue(applicationKey);
            Username = username;
            HasDetail = hasDetail;
            AccessType = accessType;
            AccessVisibility = accessVisibility;

            Write();
        }

        public void Write() {
            RuntimeLogger.Log(this);
        }

        private static string GenerateLogId() {
            return Guid.NewGuid().ToString();
        }

    }
}
