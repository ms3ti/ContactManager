/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System.Collections;
using System.Collections.Generic;
using System.Linq;
using OutSystems.Application.Core.Http;

namespace OutSystems.Application.Initialization.ASPNet.Http {
    public class ASPNetHttpItemDictionary : IHttpItemDictionary {

        private System.Web.HttpContext wrappedContext;

        public ASPNetHttpItemDictionary(System.Web.HttpContext wrappedContext) {
            this.wrappedContext = wrappedContext;
        }

        public object this[object key] {
            get {
                return wrappedContext.Items.Contains(key) ? wrappedContext.Items[key] : null;
            }
            set {
                wrappedContext.Items[key] = value;
            }
        }

        public int Count => wrappedContext.Items.Count;

        public ICollection<object> Keys => wrappedContext.Items.Keys.Cast<object>().ToList();

        public ICollection<object> Values => wrappedContext.Items.Values.Cast<object>().ToList();

        public bool IsReadOnly => wrappedContext.Items.IsReadOnly;

        public bool ContainsKey(object key) => wrappedContext.Items.Contains(key);

        public T GetValueOrDefault<T>(object key, T defaultValue = default(T)) {
            return ContainsKey(key) ? (T)wrappedContext.Items[key] : defaultValue;
        }

        public IEnumerator<KeyValuePair<object, object>> GetEnumerator() {
            return GenericDictionary.GetEnumerator();
        }

        IEnumerator IEnumerable.GetEnumerator() {
            return GetEnumerator();
        }

        public bool Remove(object key) {
            if (key is KeyValuePair<object, object>) {
                key = ((KeyValuePair<object, object>)key).Key;
            }

            if (!ContainsKey(key)) {
                return false;
            }

            wrappedContext.Items.Remove(key);
            return true;
        }

        public bool TryGetValue(object key, out object value) {
            value = null;

            if (!ContainsKey(key)) {
                return false;
            }

            value = wrappedContext.Items[key];
            return true;
        }

        public void Add(KeyValuePair<object, object> item) {
            wrappedContext.Items.Add(item.Key, item.Value);
        }

        public bool Contains(KeyValuePair<object, object> item) {
            return GenericDictionary.Contains(item);
        }

        public void CopyTo(KeyValuePair<object, object>[] array, int arrayIndex) {
            GenericDictionary.CopyTo(array, arrayIndex);
        }

        public bool Remove(KeyValuePair<object, object> item) {
            var context = wrappedContext;

            if (!context.Items.Contains(item.Key)) {
                return false;
            }

            context.Items.Remove(item.Key);
            return true;
        }

        public void Add(object key, object value) {
            wrappedContext.Items.Add(key, value);
        }

        public void Clear() {
            wrappedContext.Items.Clear();
        }

        private IDictionary<object, object> GenericDictionary {
            get {
                var context = wrappedContext;
                return context.Items.Keys.Cast<object>().ToDictionary(key => key, key => context.Items[key] as object);
            }
        }
    }
}
