/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System;
using System.Configuration;
using System.Diagnostics;
using OutSystems.HubEdition.DatabaseProvider.Oracle.ConfigurationService;
using OutSystems.HubEdition.Extensibility.Data.Platform.Configuration;
using OutSystems.RuntimeCommon;
using OutSystems.RuntimeCommon.Log;

namespace OutSystems.HubEdition.DatabaseProvider.Oracle.Platform.Configuration {

    internal class TestConnectionUtils {

        internal static int QueryTimeout = 30;

        internal static bool CheckTablespaceExists(RuntimeDatabaseConfiguration runtimeConf, bool forAdminUser, out string friendlyMessage) {
            bool exists = InnerCheckTablespaceExists(runtimeConf, runtimeConf.Tablespace, out friendlyMessage);
            if (exists && forAdminUser) {
                exists = InnerCheckTablespaceExists(runtimeConf, runtimeConf.TablespaceIndex, out friendlyMessage);
            }
            return exists;
        }

        private static bool InnerCheckTablespaceExists(RuntimeDatabaseConfiguration runtimeConf, string tablespacename, out string friendlyMessage) {
            friendlyMessage = "Test completed successfully.";
            using (var conn = runtimeConf.DatabaseProvider.GetIntegrationDatabaseServices(runtimeConf).TransactionService.CreateConnection()) {
                using (var cmd = conn.CreateCommand()) {
                    cmd.CommandTimeout = QueryTimeout;
                    try {
                        cmd.CommandText = string.Format("SELECT COUNT(1) FROM USER_TABLESPACES WHERE TABLESPACE_NAME = '{0}'", tablespacename);
                        bool exists = Convert.ToBoolean(cmd.ExecuteScalar());
                        if (!exists) {
                            friendlyMessage = string.Format("Tablespace {0} does not exist", tablespacename);
                            EventLogger.WriteError(string.Format("Error testing connection to the database: {0}", friendlyMessage));
                            return false;
                        }
                    } catch (Exception e) {
                        friendlyMessage = e.Message;
                        EventLogger.WriteError(string.Format(
                            "Error testing connection to the database: {0}\n\n{1}\n{2}",
                            e.Message, e.StackTrace, (new StackTrace(true))));
                        return false;
                    }
                }
                return true;
            }
        }

        internal static bool CheckLinguisticSorting(RuntimeDatabaseConfiguration runtimeConf, out string friendlyMessage) {
            friendlyMessage = "Test completed successfully.";
            if (string.IsNullOrEmpty(runtimeConf.NLS_Sort)) {
                // No Linguistic sort defined. It's valid
                return true;
            }
            using (var conn = runtimeConf.DatabaseProvider.GetIntegrationDatabaseServices(runtimeConf).TransactionService.CreateConnection()) {
                using (var cmd = conn.CreateCommand()) {
                    cmd.CommandTimeout = QueryTimeout;
                    try {
                        cmd.CommandText = $"select 1 from dual where nlssort(' !#%&()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`~', 'nls_sort = {runtimeConf.NLS_Sort}') = nlssort(' !#%&()*+,-./0123456789:;<=>?@abcdefghijklmnopqrstuvwxyz[\\]^_`~', 'nls_sort = {runtimeConf.NLS_Sort}')";
                        bool exists = Convert.ToBoolean(cmd.ExecuteScalar());
                        if (!exists) {
                            friendlyMessage = string.Format(@"The '{0}' linguistic sort is not supported.
OutSystems supports linguistic sorts that are Latin case-insensitive. However, the '{0}' linguistic sort does not fulfill this requirement.
Consider using the corresponding multilingual linguistic sort instead, which is Latin case-insensitive.", runtimeConf.NLS_Sort );
                            EventLogger.WriteError(string.Format("Error testing connection to the database: {0}", friendlyMessage));
                            return false;
                        }
                    } catch (Exception e) {
                        friendlyMessage = e.Message;
                        EventLogger.WriteError(string.Format(
                            "Error testing connection to the database: {0}\n\n{1}\n{2}",
                            e.Message, e.StackTrace, (new StackTrace(true))));
                        return false;
                    }
                }
                return true;
            }
        }

        internal class TestConnectionResult {
            public bool success { get; }
            public string message { get; }

            public TestConnectionResult(bool success, string message) {
                this.success = success;
                this.message = message;
            }
        }


        internal static bool DoAdditionalChecks(out string friendlyMessage, TestConnectionResult result, RuntimeDatabaseConfiguration runtimeConf, bool checkLinguisticSorting = true, bool checkTablespaceExists = true) {
            var success = result.success;
            friendlyMessage = result.message;

            if (checkLinguisticSorting && success) {
                success &= CheckLinguisticSorting(runtimeConf, out friendlyMessage);
            }

            if (checkTablespaceExists && success) {
                success &= CheckTablespaceExists(runtimeConf, false, out friendlyMessage);
            }

            if (success && result.message.StartsWithIgnoreCase("Oracle version")) {
                friendlyMessage = $"{friendlyMessage}{Environment.NewLine}{Environment.NewLine}Warning: {result.message}";
            }

            return success;
        }
    }
}