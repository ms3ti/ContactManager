/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System.Net;
using Oracle.ManagedDataAccess.Client;

namespace OutSystems.HubEdition.DatabaseProvider.Oracle.Platform.Configuration {

    internal class ConnectionStringHelper {

        private const int DefaultPortValue = 1521;

        internal const string SourceFormat = "{0}/{1}";
        internal const string SourceWithPortFormat = "{0}:{1}/{2}";

        public static string ExtractUsername(string connectionString) {
            string dbUserName = string.Empty;

            if (!string.IsNullOrEmpty(connectionString)) {
                OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder(connectionString);
                dbUserName = connectionStringBuilder.UserID;
            }

            return dbUserName;
        }

        public static string ReplaceUsername(string connectionString, string username) {
            if (string.IsNullOrEmpty(username)) {
                return connectionString;
            }

            if (string.IsNullOrEmpty(connectionString)) {
                return string.Empty;
            }

            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder(connectionString);
            connectionStringBuilder.UserID = username;

            return connectionStringBuilder.ToString();
        }

        public static string ExtractPassword(string connectionString) {
            string password = string.Empty;

            if (!string.IsNullOrEmpty(connectionString)) {
                OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder(connectionString);
                password = connectionStringBuilder.Password;
            }

            return password;
        }

        public static string ReplacePassword(string connectionString, string password) {
            if (string.IsNullOrEmpty(password)) {
                return connectionString;
            }

            if (string.IsNullOrEmpty(connectionString)) {
                return string.Empty;
            }

            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder(connectionString);
            connectionStringBuilder.Password = password;

            return connectionStringBuilder.ToString();
        }

        private static void Credentials(NetworkCredential cred, OracleConnectionStringBuilder connectionStringBuilder) {
            Credentials(cred.UserName, cred.Password, connectionStringBuilder);
        }

        private static void Credentials(string username, string password, OracleConnectionStringBuilder connectionStringBuilder) {
            if (!string.IsNullOrEmpty(username)) {
                connectionStringBuilder.UserID = username;
            }
            if (!string.IsNullOrEmpty(password) && !string.IsNullOrEmpty(username)) {
                connectionStringBuilder.Password = password;
            }
        }

        private static void DataSource(string host, int port, string serviceName, string tnsName, bool isServiceName, OracleConnectionStringBuilder connectionStringBuilder) {
            if (isServiceName) {
                if (port == DefaultPortValue) {
                    connectionStringBuilder.DataSource = string.Format(SourceFormat, host, serviceName);
                } else {
                    connectionStringBuilder.DataSource = string.Format(SourceWithPortFormat, host, port, serviceName);
                }
            } else {
                connectionStringBuilder.DataSource = tnsName;
            }
        }

        private static string AppendAdvanced(string connectionString, string advanced) {
            if (!connectionString.EndsWith(";") && !string.IsNullOrEmpty(connectionString)) {
                connectionString += ";";
            }
            connectionString += advanced;
            return connectionString;
        }

        internal static string Build(NetworkCredential cred, string host, int port, string serviceName, string tnsName, bool isServiceName, string adv) {
            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder();
            Credentials(cred, connectionStringBuilder);
            DataSource(host, port, serviceName, tnsName, isServiceName, connectionStringBuilder);
            return AppendAdvanced(ConnectionStringBuilderToString(connectionStringBuilder), adv);
        }

        internal static string Build(NetworkCredential cred, string host, int port, string serviceName, string tnsName, bool isServiceName) {
            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder();
            Credentials(cred, connectionStringBuilder);
            DataSource(host, port, serviceName, tnsName, isServiceName, connectionStringBuilder);
            return ConnectionStringBuilderToString(connectionStringBuilder);
        }

        internal static string Build(string username, string password, string host, int port, string serviceName, string tnsName, bool isServiceName) {
            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder();
            Credentials(username, password, connectionStringBuilder);
            DataSource(host, port, serviceName, tnsName, isServiceName, connectionStringBuilder);
            return ConnectionStringBuilderToString(connectionStringBuilder);
        }

        internal static string Build(string username, string password, string adv) {
            OracleConnectionStringBuilder connectionStringBuilder = new OracleConnectionStringBuilder();
            Credentials(username, password, connectionStringBuilder);
            return AppendAdvanced(ConnectionStringBuilderToString(connectionStringBuilder), adv);
        }
        
        // Note: RSAT-2474 - We cannot use connectionStringBuilder.ToString() directly because we need to create the 
        // connection string with the same order of parameters as the older versions of the platform because of upgrades.
        private static string ConnectionStringBuilderToString(OracleConnectionStringBuilder connectionStringBuilder) {
            return $"user id={connectionStringBuilder.UserID};" +
                   $"password={connectionStringBuilder.Password};" +
                   $"data source={connectionStringBuilder.DataSource}";
        }
    }
}