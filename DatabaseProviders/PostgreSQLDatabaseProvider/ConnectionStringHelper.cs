/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

using System.Net;
using Npgsql;

namespace OutSystems.HubEdition.DatabaseProvider.PostgreSQL.Configuration {

    internal class ConnectionStringHelper {

        public static string ExtractUsername(string connectionString) {
            string dbUserName = string.Empty;

            if (!string.IsNullOrEmpty(connectionString)) {
                NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder(connectionString);
                dbUserName = connectionStringBuilder.Username;
            }

            return dbUserName;
        }

        public static string ReplaceUsername(string connectionString, string username) {
            if (string.IsNullOrEmpty(username)) {
                return connectionString;
            }

            if (string.IsNullOrEmpty(connectionString)) {
                return string.Empty;
            }

            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder(connectionString);
            connectionStringBuilder.Username = username;

            return connectionStringBuilder.ToString();
        }

        public static string ExtractPassword(string connectionString) {
            string password = string.Empty;

            if (!string.IsNullOrEmpty(connectionString)) {
                NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder(connectionString);
                password = connectionStringBuilder.Password;
            }

            return password;
        }

        public static string ReplacePassword(string connectionString, string password) {
            if (string.IsNullOrEmpty(password)) {
                return connectionString;
            }

            if (string.IsNullOrEmpty(connectionString)) {
                return string.Empty;
            }

            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder(connectionString);
            connectionStringBuilder.Password = password;
            
            return connectionStringBuilder.ToString();
        }

        private static void Credentials(NetworkCredential cred, NpgsqlConnectionStringBuilder connectionStringBuilder) {
            Credentials(cred.UserName, cred.Password, connectionStringBuilder);
        }

        private static void Credentials(string username, string password, NpgsqlConnectionStringBuilder connectionStringBuilder) {
            if (!string.IsNullOrEmpty(username)) {
                connectionStringBuilder.Username = username;
            }
            if (!string.IsNullOrEmpty(password) && !string.IsNullOrEmpty(username)) {
                connectionStringBuilder.Password = password;
            }
        }

        private static void DataSource(string host, int port, string database, string schema, NpgsqlConnectionStringBuilder connectionStringBuilder) {
            if (!string.IsNullOrEmpty(host)) {
                connectionStringBuilder.Host = host;
            }
            connectionStringBuilder.Port = port;
            if (!string.IsNullOrEmpty(database)) {
                connectionStringBuilder.Database = database;
            }
            if (!string.IsNullOrEmpty(schema)) {
                connectionStringBuilder.SearchPath = schema;
            }
        }

        private static string AppendAdvanced(string connectionString, string advanced) {
            if (!connectionString.EndsWith(";") && !string.IsNullOrEmpty(connectionString)) {
                connectionString += ";";
            }
            connectionString += advanced;
            return connectionString;
        }

        internal static string Build(NetworkCredential cred, string host, int port, string database, string schema, string adv) {
            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder();
            Credentials(cred, connectionStringBuilder);
            DataSource(host, port, database, schema, connectionStringBuilder);
            return AppendAdvanced(connectionStringBuilder.ToString(), adv);
        }

        internal static string Build(NetworkCredential cred, string host, int port, string database, string schema) {
            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder();
            Credentials(cred, connectionStringBuilder);
            DataSource(host, port, database, schema, connectionStringBuilder);
            return connectionStringBuilder.ToString();
        }

        internal static string Build(string username, string password, string host, int port, string database, string schema) {
            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder();
            Credentials(username, password, connectionStringBuilder);
            DataSource(host, port, database, schema, connectionStringBuilder);
            return connectionStringBuilder.ToString();
        }

        internal static string Build(string username, string password, string adv) {
            NpgsqlConnectionStringBuilder connectionStringBuilder = new NpgsqlConnectionStringBuilder();
            Credentials(username, password, connectionStringBuilder);
            return AppendAdvanced(connectionStringBuilder.ToString(), adv);
        }
    }
}